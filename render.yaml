default:
  title: "{{page_title}}"
  slug: "{{page_title.lower().replace(' ', '_')}}"
  notebooks:
    - example
  parameters:
    page_title: "{{settings.default_page_title}}"
  context:
    data_common.management.settings:
      - settings
  options:
    rerun: true
    hide_input: true
  upload:
    site:
      description: Example analysis generated from the repository notebook.
      order: 10
